<article>
  <div>
    <ol>
      <h1> Your Cars </h1>
      <br />

      <% @cars.each do |car| %>
        <h3 id="test1">
          <li><%= link_to "#{car.brand} - #{car.model}", car_path(car) %></li>
        </h3>
      <% end %>
    </ol>

    <%= link_to 'Add a car', new_car_path %>
  </div>
</article>
